# mini-render — 开发规约

这份文件是给**在这个仓库里写代码的人和 AI** 看的行为守则。
项目本身在讲什么，看 `README.md`；架构为什么长这样，看 `docs/architecture.md`。

写在最前面的一条：**这个仓库里相当一部分代码由 AI 写。**
下面每一条约束都不是风格偏好，而是针对"AI 写图形算法时的典型失败形态"
定下的。每一条后面都写了它防的是什么。

---

## 0. 一条总原则

**能用机器检查的约定，就不要靠记。**

这个项目里三种机器检查，改代码前先知道它们存在：

```sh
make check      # = check-headers + lint + test
make lint       # grep 规则，见 scripts/lint.sh
make test       # 不变量检查，见 tests/
```

如果你发现自己在写"记得要……"这样的注释，先想想能不能变成
`scripts/lint.sh` 里的一行。上游 mini-wayland 有个现成的反面教材：
四个工具头文件躺了很久没人用，事后查出来是因为它们编译不过告警集合 ——
**没有人记录，也没有任何检查会发现**，所以它看起来像纪律问题，
其实是构建配置问题。

---

## 1. 加一节课

```sh
./scripts/new-lesson.sh 02-wu-lines "Xiaolin Wu antialiased lines" 1991 animated
```

不要手写目录。骨架统一的意义在于：AI 起手会模仿上一个例子，
骨架有差异，差异就会被一路复制下去。

### 输入：走 ssh 会话的 stdin

开发环境是两套设备：**编码设备**（写代码、rsync、开着 ssh，手放在这里）
和**开发板**（自带键鼠显示器，画面出在这块屏幕上）。

循环是：改代码 → rsync → 切 tab → ssh → `make` → `sudo ./bin/... run` →
**转头看板子的显示器**。注意"转头"只用来看，不用来操作。

所以输入走 stdin 的 raw 模式。这**不是**因为 evdev 不可行 ——
板子上确实插着键盘，`libinput` 完全读得到，Step 5 就要走那条路。
取舍是**手放在哪**：走 evdev 得整个人挪到板子那边去按键，
而编辑器、日志、git 全在编码设备上。

顺带两个点：

- 板子键盘敲下去，字符进的是**板子的 tty**，不是你的 ssh 会话。
  画面在那块屏幕上会让人下意识去按那边的键盘，按了没反应不是 bug。
- **`-b kms` 跑着的时候别碰板子的键盘。** 屏幕被我们占着，
  那个 VT 的登录 shell 还活着且看不见回显 —— 敲到回车就执行了。
  正经解法是 VT / session 管理，属于 mini-wayland，见
  `docs/architecture.md` 第 7 节。

课通过 `params.input()` 拿到解码后的状态，**不碰终端**（`make lint` 会查）。
Step 5 做出 `mw/input/` 之后，harness 会改成从那边填同一个 `Input`，
**课一行都不用改** —— 这正是把输入放在 harness 而不是课里的原因。

两条要记住的：

- **状态反馈必须画在画面上，不能打日志。** 眼睛在板子的屏幕上，
  低头看终端就错过画面了。lesson 01 的做法是左下角一排刻度。
- **`down()` 是近似的。** 终端不报按键抬起，只能用"最后一次事件后
  120ms 内算按住"来模拟，而且自动重复有初始延迟。按住方向键时
  会先动一下、停一下、再连续动。这是终端的行为，不是 bug。

### 课只能做一件事：往一块 `mr::Surface` 上画像素

```c++
void render(const mr::Surface& out, const mr::LessonParams& params);
MR_LESSON("NN-slug", "Title", year, mr::Cadence::Animated, render);
```

课**不允许**：写 `main()`、碰 `argc`/`argv`/`getenv`/`sigaction`、
`#include <mw/...>`、手写 ANSI 转义。这七条由 `make lint` 强制，
不是建议。违反任何一条都会让这节课没法在单测里跑 ——
单测里没有 argv，也没有屏幕。

### 算法不写在 `lesson.cpp` 里

`lesson.cpp` 的职责是**把算法画成一个人能看出对错的画面**。
算法本身放 `include/mr/<领域>/<名字>.hpp` + `src/<领域>/<名字>.cpp`。

理由是可测性：`tests/` 要直接调算法，而不是"跑一遍课再看内存"。
判据很简单 —— 如果这个算法没法在没有 `Surface` 的情况下被调用一次，
那它被放错地方了。参考 `mr/raster/line.hpp` 的 `trace_line()`：
它把像素交给回调，所以测试能直接断言"选出了哪一串像素"。

---

## 2. 课的 README 怎么写

`lessons/XX-YYY/README.md` 是**教材**，不是项目文档。读者是一个想学计算机图形学的人，他没有这个仓库的任何背景，很可能只是互联网上随手搜到了这一篇。

验收判据只有一条：**把这篇 README 单独拷出去发到别处，它仍然是一篇完整、自洽、读得懂的图形学讲义。** 凡是拷出去之后变得莫名其妙的内容，都不该写在里面。

下面每一条都是实际写砸过的。

### 写什么

- 这个算法要解决的成像问题是什么，为什么最直接的想法不够用
- 完整推导。中间的代数变形一步都不要跳，跳掉的那步就是读者卡住的地方
- 一个手推得动的小例子，配一张逐步过程表
- 算法的输出在屏幕上长什么样，以及为什么长那样（走样、阶梯、密度不均这些视觉现象要正面解释，它们往往是读者的第一个疑问）
- 术语。读者可能连"光栅化"都是第一次见，第一次出现就地解释清楚，别指望他去查

### 不写什么

**不写项目介绍。** 不解释目录结构、构建系统、某个头文件里某个函数的用法、某层抽象为什么这么切、和上游显示库怎么对接。这些是 `docs/` 和这份文件的活，写进教材就是跑题。

**不写工程实践。** 怎么做测试、什么叫不变量、怎么故意注入 bug 验证测试有效、挂死的进程为什么比画错的画面难查。这些内容本身没错，但它们不是图形学，读者是来学画线的。

**不写仓库术语。** 尽量不出现 `mr::Surface`、`mini-wayland`、`MR_BUG`、`make lint` 这类只有本仓库读者认识的名字。需要提到"一块可以逐像素写的图像内存"时，就叫它像素平面。怎么把这节课跑起来，放在结尾三五行带过就够了。

**不写跨节引用。** 禁止出现"这个第 7 节会讲""回忆一下第 2 节""见下文"。读者会真的跳过去找，回来就断了。需要用到的东西，就在用到它的地方讲一遍；宁可把一段话重复两遍，也不要让人翻页。

**不写元叙述。** "下面这张表是真实跑出来的不是手算的""这一节特别重要""这一点值得单独说""这是工程上最难查的一类 bug"。删掉之后信息量一点没变的句子，就是应该删掉的句子。要强调，靠把内容讲清楚来强调。

### 排版

**不按 80 字符折行。** 一个自然段写成一行，折行交给编辑器。80 字符是给打字机和 VT100 定的宽度，在宽屏上会把正文挤成一根细柱子，读起来比长行更累。

**不用破折号做插入语。** 中文里连续两个 `——` 读起来像喘气，而且是 AI 腔最明显的特征。该断句就断句，该补充就用括号，或者干脆另起一句。

代码块、表格、ASCII 示意图都欢迎，它们通常比一整段描述有效。复杂的叙述最好用一个小例子手把手走一遍

### 图

图放在 `lessons/XX-YYY/picture/`，格式不限，但优先 code as picture, 即代码生成图，比如SVG：是文本，能 diff，放大不糊。README 里用相对路径引用：

```markdown
![浅斜率下每一列选哪个像素](picture/octant-1.svg)
```

一张图能省掉的段落，就不要用段落写。像素网格、方向分区、误差的几何含义、阶梯为什么有长有短，这些画出来一眼就懂，写成文字要三百字还讲不明白。

---

## 3. 写完算法，先把它写错一次

**这是本项目最重要的一条实践。**

给算法加不变量测试之后，**故意注入一个 bug，确认测试真的会红**。
没做过这一步的测试等于没有。

理由：AI 写的光栅化循环通常"看起来完全正确"，错误集中在
少数几个卦限、或者只在某个退化比例下差一格。而如果期望值
也是同一个 AI 生成的，逐像素比对等于自己批改自己。

`tests/test_line.cpp` 里对 Bresenham 注入了六种典型错误，
六种全部被抓到 —— 其中四种表现为**根本不返回**，而不是画错。
这个比例说明了为什么"跑一下看画面对不对"不够。

### 什么是不变量

不是"这条线应该画出这些像素"，而是从算法**定义**推出来、
与实现无关的性质：

| 性质 | 抓什么 |
| --- | --- |
| 端点必被画上，且是首尾 | 起止条件写反 |
| 相邻像素 8-连通 | 某个卦限一步走两格 |
| 无重复像素 | 判别式更新顺序错 |
| 步数正好 `max(\|dx\|,\|dy\|)+1` | 差一错误（屏幕上看不出来） |
| 正反向画出的像素**集合**相同 | 平局规则不对称 |
| 不写出 `width()` 之外（stride padding） | 用 `width*4` 当行跨距 |

后面的课有各自的不变量：面积守恒、能量守恒、深度单调、
采样收敛到解析解。**每节课都必须至少有一条。**

---

## 4. 死循环要变成报错

`MR_BUG(msg)`（`mr/bug.hpp`）用来标记"只可能由实现错误到达"的分支。
debug 下 abort 并打位置，release 下什么也不做。

**迭代循环必须有一个来自定义的确切上限。**
不是"给个大点的安全余量"，是把"正确实现走多少步"算出来当上限：
Bresenham 是 `max(|dx|,|dy|)+1`，光线步进是最大步数，
路径追踪是最大反弹次数。对正确实现零影响，对写错的实现
把"挂死"变成一行明确的报错。

这条是实测出来的：Bresenham 的六种注入错误里有四种让循环永不返回。
挂死的进程比画错的画面难查得多 —— 没有输出、没有退出码，
在 CI 里只表现为超时。

---

## 5. stride

**本项目最容易犯、也最难一眼看出的错。**

行跨距用 `Surface::stride()`，定位像素用 `Surface::at()` / `pixel()`。
**任何地方都不允许出现 `width * 4`。** `make lint` 会查。

为什么它难发现：offscreen 后端下 `stride == width * 4`，
所以写错了在开发机上完全正常；上真硬件才会画面倾斜，
而"倾斜"看起来像几何算错了，不像内存布局问题。

同理，`blit()` 必须逐行拷贝，不能一次 `memcpy` 整块。

---

## 6. 浮点用在哪、不用在哪

早期的课（Bresenham、Cohen–Sutherland、扫描线填充）的**卖点就是不用浮点**。
在这些课里出现 `float`/`double`/除法，等于把课讲没了。

界线是：**算端点可以用浮点，光栅化不行。**
`lessons/01-bresenham` 里用 `sin`/`cos` 算辐条端点 —— 那是在
构造输入，不是在算法内部。别看见 `cos` 就以为算法用了浮点。

后面的课（着色、路径追踪）当然要用浮点，那时讨论的是精度和采样，
不是要不要用。release 构建**不开** `-ffast-math`，就是为了那些讨论。

---

## 7. 语言

- **注释可以中文，字符串字面量必须英文。** 日志、错误消息、
  命令行输出、`MR_BUG` 的消息一律英文。`make lint` 会查。
- 和上游 mini-wayland 一致。两个项目的代码会互相搬。

## 8. 告警

告警集合与 mini-wayland **逐条一致**，不是"差不多"。
`-Werror` 是默认，不是可选开关。

两个不能开的：`-Wpedantic`（mini-wayland 的 `TRY()` 用了
GNU statement-expression）、`-Wpadded`（噪音）。

新代码撞上告警时，**先假设告警是对的**。这套告警是按
"AI 的幻觉主要表现为看起来对的类型转换和忘了初始化的成员"
选的，命中率很高。

---

## 9. 和 mini-wayland 的关系

mini-render **依赖** mini-wayland，不是它的 fork。

- 认识 `mw::` 的只有 `src/main.cpp`。`make lint` 会查。
- `mr::Surface` 是唯一的接缝（`mr/surface.hpp`）。
  上游改了 `mw::display::Frame`，只需要改 `surface_from()` 一个函数。
- submodule 钉在 tag 上，**不跟上游 `main`** ——
  mini-wayland 的 `mw/drm` 与 `mw/render` 明确标了"会变"。
- 在这边发现的显示层 bug，修到上游去，不要在这边打补丁绕开。

---

## 10. 提交前

```sh
make check                              # check-headers + lint + test
make BUILD=release -j                   # release 也要过 -Werror
make SANITIZE=1 test                    # ASan + UBSan
./build/debug/bin/mini-render list      # 新课出现在列表里了吗
make run LESSON=NN                      # 画面对吗
```

最后一条特别提醒：**`MR_LESSON` 的注册靠静态初始化。**
如果新课没出现在 `list` 里，八成是它的 `.o` 没被链进去。
Makefile 里 lesson 对象是单独一组、直接链进可执行文件，
就是为了这个 —— 打成 `.a` 的话链接器不会拉一个没有符号被引用的
目标文件进来，课会**静默消失，没有任何报错**。

---

## 11. 写给 AI 的额外几条

- **不要为了让测试通过而改测试。** 不变量是从定义推出来的；
  测试红了，先假设是实现错了。确实需要改不变量时，
  在 commit message 里说明为什么原来那条推错了。
- **不要在 `lesson.cpp` 里重新实现已有的算法。** 先看
  `include/mr/` 下有没有。重复实现会导致两份行为分叉的代码，
  而且分叉的那份不受 `tests/` 保护。
- **动画用 `params.dt_s` 积分，不要用 `params.time_s` 或帧号。**
  帧率会因为算法开销、丢帧、`--no-pace` 而变。用 dt 积分的动画在
  这三种情况下速度一致，用帧号递增的不会。而且暂停/继续只有用 dt
  才能从停下的地方接着走。
- **不要引入新的第三方依赖。** 目标环境网络不稳、资源有限。
  标准库 + mini-wayland 就是全部。需要随机数、矩阵、
  图像编解码时自己写一个最小的 —— 那些本来也是课的一部分。
- **不确定时，把判断所需的信息打出来，不要替人下结论。**
  这条从 mini-wayland 继承而来。